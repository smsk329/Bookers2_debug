$(".new-comment").html("<%= j(render 'book_comments/form', book: @book, book_comment: @book_comment) %>");
$(".comments-area").html("<%= j(render 'book_comments/comments', book: @book) %>");
<!--フォームを送信したあとにフォームの値を空にします-->
$("textarea").val('');